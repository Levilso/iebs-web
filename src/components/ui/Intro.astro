---
const {background, h1} = Astro.props;
---

<div id="container"
    class={`
        -z-10
        w-full h-screen object-cover object-top
        pt-25
        bg-linear-to-r from-[#4cd1e9] to-(--brand-color)
        bg-cover bg-top
    `}
>
    <section id="hero">
		<h1 class="font-semibold text-white text-4xl p-5 text-center">
		{h1}
        </h1>
    </section>
</div>